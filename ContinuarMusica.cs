using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ContinuarMusica : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
       // GameObject.FindGameObjectWithTag("audioObj").GetComponent<AudioSource>().PlayMusic();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
